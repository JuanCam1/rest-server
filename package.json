{
  "name": "rest-server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "biome format --write ./src",
    "lint": "biome lint --write ./src"
  },
  "dependencies": {
    "@prisma/client": "6.6.0",
    "hono": "^4.7.7",
    "prisma": "^6.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest"
  }
}